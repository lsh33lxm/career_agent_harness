# 原始岗位数据审计

状态：**阻塞，等待真实路径**。

本轮要求提供的岗位数据库路径仍是字面占位符 `<JOB_DB_PATH>`，当前仓库及环境中没有可验证的真实路径。因此没有生成岗位数量、时间跨度、字段或公开样本结论，也没有把 `tmp_0019.db`、Legacy Agent Radar 或其他运行时数据库冒充原始岗位库。

收到真实路径后，使用以下只读命令生成审计：

```powershell
python scripts/audit_job_db.py "D:\真实\岗位库.db" --output docs/data-audit.md
```

脚本仅读取 SQLite/CSV/TSV，记录 SHA-256、表/字段/行数，并按字段名给出保守的“可公开 / 需脱敏 / 不可公开 / 需人工审阅”建议；它不会导出职位原文、联系人、凭证或完整数据集。

在路径与来源再分发权利明确前，公开演示数据包保持未生成状态，避免伪造“6000+”统计或误发布第三方职位内容。
