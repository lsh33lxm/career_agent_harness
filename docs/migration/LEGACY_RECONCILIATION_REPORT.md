# Legacy reconciliation — 2026-09-21

Status: preservation and disposable provenance rehearsal verified; canonical cutover
NOT APPROVED / NOT PERFORMED. Source remains read-only. Contract 0.17.0 / D-027.

## Frozen inputs and preservation

Inventory SHA-256: `0958a30658003fd67efdce7722b1870a4c9963744e1a21f1693961b289dc43ce`.
2,426 paths / 377,785,991 bytes; zero skipped links and zero hash mismatches.
Metadata signature before/after inventory, content inspection and final rehearsal:
`1f105e51963c59b11b4624679ded718ae267c509dda64480298c35d1e929cd19`.

Final archive index SHA-256:
`efb43972490aca6a249bf0c74a52ea9bfb5756465d7fa408ea708a611841536c`.
Both exact inventory and index are themselves content-addressed artifacts. Every
original path, mtime, length/hash, source class, disposition and reason remains in
the index. Duplicate content retains each origin. No raw files were committed.

| Disposition | Paths | Meaning |
| --- | ---: | --- |
| preserved | 2205 | Content in existing Artifact Store; no claim authority promotion |
| excluded | 204 | 203 test-workspace fixtures; 1 authentication/session ledger |
| deferred | 17 | 16 generated Python bytecode files and 1 downloaded JavaScript with credential-like assignment marker |

Deferred originals remain in Legacy; no deletion or complete-independence claim for
those 17. Their exact manifest entries remain recoverable. The JavaScript marker
was not treated as a proven live secret or printed; no guess/automatic sanitization.
Generated bytecode is not required by the new runtime and was not executed.
Three source-code paths containing profile/tokens were inspected as code and allowed
only by exact content-hash exceptions. Credential directories, fixtures and embedded
credential markers cannot use this exception. Six ZIP containers were boundedly
inspected, including nested member checks, before explicit archival selection.

Data Root uses existing AppPaths. On this packaged Windows host,
`%LOCALAPPDATA%/AgentCareerHarness` resolves to
`C:/Users/lxm33/AppData/Local/Packages/OpenAI.Codex_2p2nqsd0c76g0/LocalCache/Local/AgentCareerHarness`.
The normal `C:/Users/lxm33/AppData/Local/AgentCareerHarness` spelling was verified with
`samefile` as the same physical directory. Artifact locations use digest fan-out.
Production `career_harness.db` was not imported or cut over.

## File-level reconciliation

| Candidate grouping | Groups / paths | Disposition |
| --- | ---: | --- |
| Same basename key and same hash | 720 groups | Byte-equivalence proven; origin paths retained |
| Same basename key, different hashes | 45 groups | All versions preserved or explicitly excluded/deferred; canonical identity deferred |
| Cross-path identical hash | 712 groups | Content-addressed deduplication, not source identity collapse |
| Snapshot candidates | 286 paths | Historical snapshots retained; no automatic newest-wins |
| Version-family candidates | 667 groups | Heuristic grouping only; no inferred canonical family |

The generic reconciliation report intentionally says archive-member inspection is
not performed by that function. Separately, the real bounded content inspection
recorded 6,703 member visits (including repeated nested containers), 1,031 distinct
member hashes, 931 hashes seen more than once. These visits are not unique files or
business records. Exact container hash/member name/member hash pairs are in the
local inspection receipt; no extraction paths were followed and no archive scripts ran.
Derived-copy relationships remain candidates unless byte equality was established.

## Structured conflicts requiring an authority decision

See [data baseline](../prd/DATA_BASELINE_v1.0.md) for per-file anchors and denominators.

- 35 data CN JD URLs also occur in codex CN records, with S/C grade differences.
  Both snapshots remain; no directory is silently chosen as canonical.
- 231 work interview IDs map to normalized records; B→A in 29 and D→C in 15.
  Preserve old labels; mapping to Core authority is deferred pending source definitions.
- 121 incremental question IDs already exist, 82 with a changed canonical_question_id.
  No append-as-new or overwrite; retain mapping versions.
- 72 overseas JD entries are role-family aggregates, 54 with content_scanned=false.
  They are not individual JobRevision records.
- 270 personal coding rows are plans with zero attempts. They never become market
  evidence, completed practice, personal mastery or Resume facts.
- Excel: 11 paths / 6 content versions, preserved intact. Missing formula caches
  stay missing; new runtime never depends on Excel recalculation.

All above are explicitly DEFERRED for business authority, while bytes are preserved.
This report does not claim all source URLs/web screenshots or every structured edge
has been semantically verified. A/B/C/D/S stay literal legacy grades; no automatic
mapping to Code Verified, Document Supported, User Confirmed or AI Inferred.

## Rehearsal and restore

Implementation cd7b30e -> merge b13d0f3; independent review APPROVE after transaction
rollback correction and exact source-code exception review. Focused 18 passed.

| Check | Verified result |
| --- | --- |
| Disposable schema | Fresh 0001..0013; explicit rehearsal guard, production path refused |
| Artifact rows | 1141 |
| Source / Snapshot / EvidenceRef rows | 2205 each |
| Reimport | Same report and logical rows, no duplication |
| Fresh second DB | Same logical hash |
| DB integrity / foreign keys | Passed |
| Exact provenance / artifact size and SHA-256 | Passed |
| Backup + restore into absent targets | Passed; artifact index/inventory included |
| Restored logical rows / reimport | Same hash and report |
| Canonical cutover | false |

Logical hash: `a3f65bfb4e06ba48b1ff5418d2b602a5d9e89557cf0f4e15a20870f8e7789252`.
Rehearsal evidence lives under Data Root `backups/legacy-20260921-rehearsal/`:
first/rebuilt/restored.rehearsal.db, backup manifest/artifacts, restored-artifacts,
report.json. Both independent fresh DBs were retained for inspection instead of
deleting the first; neither is a production database. Original Legacy is unnecessary
for restoring this preserved subset. The inspection/audit receipts are local under
integration `runtime/legacy-20260921/`; the pinned source/index bytes are in backup.

An earlier preserved subset index
`1c86c6bb739871f9be8797cb4b90b1c487709d713ec990bfd27a6d37cd6516c2`
is retained as history. A local report serialization error occurred before any DB
import; the final index includes the six subsequently inspected source/container
paths. No partial canonical writes occurred and no historical artifact was overwritten.
