{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the primary desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-close"]
}
