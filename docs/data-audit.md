# 原始岗位数据审计

状态：已完成核心岗位 CSV 的只读审计；其他文件仍需按来源许可单独审阅。

已确认只读 Legacy 路径为 `D:\0.小红书投稿\小红书稿\9.15 三期\agent_rader`。核心文件 `data/统一数据/岗位与JD数据.csv` 共 492 行，SHA-256 为 `477ed75bb81dd2a66a1b06f91c8af5bfb5997704b6a4c0d7bf19b9482eef60cd`。文件按 GB18030 兼容编码读取；源文件未被修改。

字段结论：岗位名称、公司规范名、城市层级、区域、证据等级和技能标签可在来源许可明确后用于脱敏演示；原始 URL、完整职责/要求/JD、归档路径、source_id 和任何联系人字段不进入公开数据包。

收到真实路径后，使用以下只读命令生成审计：

```powershell
python scripts/audit_job_db.py "D:\真实\岗位库.db" --output docs/data-audit.md
```

脚本仅读取 SQLite/CSV/TSV，记录 SHA-256、表/字段/行数，并按字段名给出保守的“可公开 / 需脱敏 / 不可公开 / 需人工审阅”建议；它不会导出职位原文、联系人、凭证或完整数据集。

当前公开包仅允许通过 `scripts/build_demo_dataset.py` 生成最多 24 条摘要样本；它不代表 6000+ 全量岗位，也不改变 Legacy 只读边界。
