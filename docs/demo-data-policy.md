# 演示数据政策

当前仓库不包含完整岗位数据库、全量导出、原始职位正文、联系人、凭证或运行时日志。

公开演示数据只有在真实岗位库完成只读审计、字段脱敏和来源再分发许可确认后才能生成。生成器必须稳定采样、保留匿名化来源类型和证据引用，并明确标注“历史样本，不代表实时职位，也不应直接用于投递决策”。

当前状态：等待 `<JOB_DB_PATH>` 的真实路径，因此演示数据包尚未生成；界面中的 Demo Mode 仅是离线壳层标识，不声称包含真实岗位统计。
